{
  "name": "pollbook",
  "version": "0.2.0",
  "description": "Election awareness app — every election in all 50 states, with live candidates, odds, and policy positions.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node test/unit.js && node test/pagination.js && node test/qa.js && node test/bills.js && node test/cache.js && node test/routes.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
